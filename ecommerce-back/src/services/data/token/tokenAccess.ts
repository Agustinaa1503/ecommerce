// import * jwt from 'jsonwebtoken';

// export function generateAccessToken(userId: string, userEmail: string) {
//     const playload = {
//         id: userId,
//         email: userEmail
//     }

//     const token = jwt.sign(playload, process.env.ACCESS_TOKEN_SECRET!, { expiresIn: '1h' });
//     return token;
// }